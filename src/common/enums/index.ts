export enum CementType {
  ASTM_TYPE_I = 'ASTM_TYPE_I',
}

export enum Verdict {
  PASS = 'PASS',
  MARGIN = 'MARGIN',
  FAIL = 'FAIL',
}

export enum NarrationMode {
  TEMPLATE = 'template',
  LLM = 'llm',
}

export enum RecommendationCategory {
  RAW_MIX = 'raw_mix',
  KILN = 'kiln',
  GRINDING = 'grinding',
}
